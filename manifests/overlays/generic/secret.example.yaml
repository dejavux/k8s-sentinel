# Example only — do not apply with placeholder values.
# Copy keys into a real Secret or use Helm secrets.existingSecret.
apiVersion: v1
kind: Secret
metadata:
  name: k8s-sentinel-secrets
  namespace: kube-system
type: Opaque
stringData:
  github-token: "REPLACE_ME"
  cursor-api-key: "REPLACE_ME_OPTIONAL"
