# Example only — requires 1Password Connect Operator.
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: k8s-sentinel-secrets
  namespace: kube-system
spec:
  itemPath: vaults/YourVault/items/k8s-sentinel-credentials
